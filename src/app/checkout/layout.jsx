export const metadata = {
  title: "Checkout",
  description: "Complete your order - fresh eggs delivery in Coimbatore.",
  robots: { index: false, follow: true },
};

export default function CheckoutLayout({ children }) {
  return children;
}
