export const metadata = {
  title: "Product Details",
  description: "View product details - fresh eggs from Muttaikadai, Coimbatore.",
};

export default function ProductLayout({ children }) {
  return children;
}
